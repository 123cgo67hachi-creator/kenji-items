# カタログページ構築 引き継ぎメモ（2026-07-21）

## 目的・背景

TikTokショップ用に作った動画を **YouTube Shorts / Instagram リール** へ横展開する。
末尾（CTA）だけ差し替え、「詳細はプロフィールのリンクから」に誘導する。

- **本当の狙い**：楽天で稼ぐことではなく、IG・YTのフォロワーを育てること
  - Instagram → Temuなど大型案件の固定報酬（月10万〜）を狙う
  - YouTube → ガジェット系の商品提供を狙う
- 楽天アフィリエイト収益は副産物

## 規約確認済み（重要）

- **楽天アフィリエイト認定SNS**：YouTube・Instagram・TikTokはすべて認定済み。直接リンク設置OK
- **PR表記**：「広告」等の表記があればステマ規制の要件を満たす。アフィリ先の明示（「楽天アフィリエイト」等）は不要
- **他人の商品画像の流用はNG**（楽天の商品画像をそのまま使うのは不可）→ 自分で撮影・作成した画像を使う

## 構築済みのもの

### リポジトリ
`https://github.com/123cgo67hachi-creator/kenji-items`

### 公開URL
- **カタログページ**：https://123cgo67hachi-creator.github.io/kenji-items/
- **管理ページ**：https://123cgo67hachi-creator.github.io/kenji-items/admin.html

### ファイル構成
```
kenji-items/
├── index.html          # カタログページ（自動生成・直接編集しない）
├── admin.html          # 管理ページ（商品登録・画像アップ用）
├── generate.py         # Notion → index.html を生成
├── update_notion.py    # admin経由の入力をNotionに書き戻す
├── images/             # サムネイル画像置き場
└── .github/workflows/sync.yml   # 同期ワークフロー
```

### Notion側の変更
案件管理表（DB ID: `fd7b3071-f92a-46d6-8cf4-0eab85108bf9`）に列を3つ追加済み：
- `楽天リンク`（URL型）
- `TikTokリンク`（URL型・2026-07-21追加）
- `サムネURL`（URL型）

### 同期の仕組み
- `ステータス = 投稿済み` の案件だけをカタログに表示
- 商品名末尾の①②は除去し、重複は1件にまとめる
- 毎朝 05:30 JST に自動同期（cron: `30 20 * * *` UTC）
- 手動同期は admin.html の「送信」or「画像なしで同期だけ実行」

### GitHub Secrets
- `NOTION_TOKEN` 登録済み

## 管理ページの使い方

1. https://123cgo67hachi-creator.github.io/kenji-items/admin.html を開く
2. 初回のみ GitHub Token を入力して保存（localStorage に保存される）
   - トークンは `gh auth token` で取得
3. 商品を選ぶ → TikTokショップURL／楽天URLを貼る（片方だけでも可）→ 画像をドロップ → 送信
4. 1〜2分でカタログに反映

## 現在の状態（2026-07-21 更新）

- カタログページ：**161商品**が表示中（31件ではない。案件が積み上がった）
- 楽天リンク：**0件**（全商品「準備中」表示）← 最大のボトルネック
- サムネ画像：1件（`images/汗染みできないシャツ.png`）
- 毎朝05:30の自動同期：連日 success
- admin.html：画像アップロード（contents API）は実績あり。
  workflow_dispatch も動作確認済み。`gho_` トークンには `workflow` スコープがあり、
  **セッション切れの懸念は現時点では的外れ**だった

### 2026-07-21 に修正した点

- `update_notion.py`：クエリがページネーションしておらず100件で打ち切られていた。
  投稿済みが161件あるため、101件目以降はadminから更新しても
  **無言で失敗**していた（`generate.py` は元からページネーション済み）。
  該当なしの場合は `exit 1` で明示的に落とすようにした
- `generate.py`：並び順を `created_time` → `last_edited_time` の降順へ。
  ただし案件ページは毎朝の自動処理でも更新日が動くため、更新日だけだと
  準備中の商品が上位を占めてしまう。安定ソートで
  **楽天リンクあり → その中で更新日降順** の2段階にした。
  リンクが0件の今は見た目上の変化なし。リンクを入れ始めて初めて効いてくる
- `generate.py`：同名（①②）をまとめる際、楽天リンク・サムネURLは
  入っている方を採用するマージ処理に変更

### 2026-07-21 追加：購入リンクの2本立て（楽天＋TikTokショップ）

TikTokショップのアフィリリンクはTikTokアプリ外（IG・YT・ブログ）でも使える。
リンク有効期限100日／クリック後14日以内の購入が成果対象／**購入者が商品を受け取って初めて報酬確定**。
そこでカード1枚に購入リンクを2本置けるようにした。

- **TikTokが主導線**：黒塗りボタン `TikTokで見る` を上に置く
- **楽天は副導線**：赤い枠線だけのボタン `楽天で見る` を下に置く
- TikTokショップを嫌う人の逃げ道を潰さないため、**両方あるときは必ず両方表示する**
- 片方しかない商品はその1本だけ表示。**両方空のときだけ「準備中」**
- 並び順は「楽天リンクあり」→「**どちらかのリンクあり**」を前に出す条件へ変更

※ボタン下の注記は「クーポンが出ることがあります」。
TikTokショップが常に安いわけではないため、**「お得」「最安」と断定しない**（景表法）。
実際に安い商品だけバッジを出したくなったら、Notionに`TikTok推し`チェック列を足す案がある。

関連ファイル：`generate.py`（描画）／`admin.html`（入力欄）／`update_notion.py`（書き戻し）／`sync.yml`（`tiktok_url` 受け渡し）

## 未解決・次にやりたいこと

1. **楽天アフィリリンクの取得（最優先）**
   - 161件を手作業だと1件2分でも5時間超。現実的でない
   - 楽天ウェブサービスの商品検索API（`IchibaItem/Search`）は
     アフィリエイトIDを渡すと `affiliateUrl` を直接返す。
     商品名で検索→候補を提示→選ぶ、の半自動化で1〜2時間に圧縮できる見込み
   - 必要なもの：**アプリID**（https://webservice.rakuten.co.jp/ で無料登録）＋**アフィリエイトID**
   - 注意：161件には楽天で売っていないものが多数混在
     （松屋のうなぎ／ハワイアンズ／大阪ベイスメントホテル／リンガーハット等）。
     除外リストとして扱う設計が必要
1-b. **TikTokショップのアフィリリンク取得**
   - 器（Notion列・admin入力欄・2ボタン表示）は完成済み。中身が0件
   - 紹介済み商品なのでTikTokアフィリエイトセンターから一括で取れる可能性あり（未調査）
2. **「リンクありのみ表示」への変更**
   - 今は161枚すべて「準備中」で訪問者に無価値
   - ただし**リンクを入れる前にやると0件になる**ので、必ず1の後
3. **GitHubトークンのFine-grained PAT化**
   - 寿命の問題ではなく**セキュリティ**の理由。現状は公開ページの localStorage に
     `repo` 全権＋`workflow` スコープのトークンが置かれている＝
     全リポジトリに書ける鍵が公開URLのブラウザストレージにある
   - このリポジトリのみ・contents:write + actions:write に絞ったPATへ差し替える
4. **iOSショートカット**：一度試みたが認証情報を埋め込めず断念。admin.html をホーム画面に追加する方針に切り替え済み
5. **サムネ画像の作り方**：`tiktok-thumbnail` スキルで作った画像を admin から上げる想定。ワークフロー未整備
6. **YouTube用の導線**：概要欄には楽天直リンクを貼る方針（カタログを経由しない）で合意済み。実装作業は未着手

## 決まっていること（再確認不要）

- ページタイトルは「けんじのオススメアイテム」
- PR表記は「広告・PR」のみ
- デザインの方向性はこのままでOK
- 独自ドメインは当面不要（フォロワーが育ってから検討）
- Notionは非公開のまま。公開するのは生成された静的HTMLのみ
